export class WishItem {
    constructor(public wishText: string, public isComplete: boolean = false) {
        // parameter properties is a TypeScript feature that allows you to define properties and assign them in the constructor
        // TypeScript shorthand for defining properties and assigning them in the constructor
    }
}